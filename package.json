{
    "name": "developer-platform",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "setup": "npm --prefix server install && npm --prefix client install",
        "dev": "npx -y concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev -- --host 0.0.0.0\"",
        "build": "npm --prefix client run build",
        "server": "npm --prefix server start",
        "client": "npm --prefix client run dev -- --host 0.0.0.0"
    },
    "devDependencies": {
        "concurrently": "^9.2.3",
        "typescript": "^7.0.2"
    }
}
